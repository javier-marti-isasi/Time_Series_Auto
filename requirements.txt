# Core
pandas>=2.0.0
numpy>=1.24.0

# Data I/O (parquet support for pandas)
pyarrow>=12.0.0

# Visualization
matplotlib>=3.7.0
plotly>=5.14.0
Pillow>=10.0.0

# ML / Forecasting
catboost>=1.2.0
statsmodels>=0.14.0

# PyTorch (CPU-only wheel by default)
# For GPU (CUDA 12.1), install with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CUDA 11.8, use:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Chronos (Amazon Science)
# Installs from the official GitHub repository
git+https://github.com/amazon-science/chronos-forecasting.git

# MLOps (ClearML)
clearml>=1.13.0
